# Minimal requirements for macOS - no compilation needed
# Install with: pip install --no-cache-dir -r requirements-minimal.txt

# Core dependencies
streamlit==1.31.0
anthropic==0.18.1
python-dotenv==1.0.1
pydantic==2.5.3
pydantic-settings==2.1.0

# Document processing (basic)
pypdf2==3.0.1
python-docx==1.1.0
python-pptx==0.6.23
#pandas==2.2.3
openpyxl==3.1.2

# Vector store - ChromaDB with minimal dependencies
chromadb==0.4.22

# Embeddings - using sentence-transformers
sentence-transformers==2.3.1
tiktoken==0.5.2

# LangChain components (install separately if needed)
# langchain==0.1.7
# langchain-community==0.0.20
# langchain-anthropic==0.1.1

# Optional: OpenAI (only if using OpenAI embeddings)
# openai==1.12.0
